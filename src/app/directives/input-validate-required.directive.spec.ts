import { InputValidateRequiredDirective } from './input-validate-required.directive';

describe('InputValidateRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValidateRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
